<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Game Client" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Server IP:"/>
        <TextBox Grid.Row="0" x:Name="txtIP" Margin="100,0,0,5"/>

        <Label Grid.Row="1" Content="Port:"/>
        <TextBox Grid.Row="1" x:Name="txtPort" Margin="100,0,0,5"/>

        <Label Grid.Row="2" Content="Username:"/>
        <TextBox Grid.Row="2" x:Name="txtUserName" Margin="100,0,0,5"/>

        <Label Grid.Row="3" Content="Time Limit (in seconds):"/>
        <TextBox Grid.Row="3" x:Name="txtTime" Margin="150,0,0,5"/>

        <Button Grid.Row="4" Content="Connect" Click="ConnectButton_Click" HorizontalAlignment="Center" Width="100" Margin="0,10"/>
    </Grid>
</Window>
